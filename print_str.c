#include "main.h"

/**
 *print_str - prints variadic string each character per time to stdout
 *@args: variadic parameter
 *
 *Return: number of characters printed
 */
int print_str(va_list args)
{
  //expressions;
  return (0);
}
